fastapi==0.115.0
uvicorn[standard]==0.32.0
sqlalchemy==2.0.36
pymysql==1.1.1
pydantic-settings==2.6.1
pyjwt[crypto]==2.9.0
passlib[bcrypt]==1.7.4
bcrypt==3.2.2
python-multipart==0.0.12
python-docx==1.1.2
jinja2==3.1.4
httpx==0.27.2
# 謄本結構化擷取用 Amazon Bedrock(OCR_LLM_PROVIDER=bedrock)時才會用到;沒設就不載入。
boto3==1.35.99
pymupdf==1.24.11
pillow==10.4.0
numpy<2
opencc-python-reimplemented==0.1.7

# OCR: RapidOCR (ONNX, CPU-only) is the engine for this image. PaddleOCR/paddlepaddle
# were removed - on the low-RAM NAS `import paddle` alone (~1.5GB) OOM-killed the whole
# process. RapidOCR is ~1/10 the footprint, needs no GPU / CUDA toolchain / JIT compile,
# and utils/ocr.py already falls back to it when PaddleOCR is unavailable.
# (The GPU dev machine runs from its own venv, not this Dockerfile, and can still use
#  PaddleOCR there.)
rapidocr-onnxruntime==1.3.24
onnxruntime==1.19.2
openpyxl==3.1.5

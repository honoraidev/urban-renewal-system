fastapi==0.115.0
uvicorn[standard]==0.32.0
sqlalchemy==2.0.36
pymysql==1.1.1
pydantic-settings==2.6.1
pyjwt==2.9.0
passlib[bcrypt]==1.7.4
bcrypt==3.2.2
python-multipart==0.0.12
python-docx==1.1.2
jinja2==3.1.4
httpx==0.27.2
pymupdf==1.24.11
pillow==10.4.0
paddlepaddle>=2.5,<2.7
paddleocr>=2.7,<3.0
numpy<2
opencc-python-reimplemented==0.1.7

# GPU acceleration - onnxruntime-gpu itself is deliberately NOT listed here
# (see Dockerfile: it's installed as a separate --force-reinstall step *after* this file).
# These nvidia-*-cu12 wheels (just the .so files, no system CUDA toolkit needed) are safe
# to list normally since nothing else depends on the plain `onnxruntime` name.
nvidia-cudnn-cu12
nvidia-cublas-cu12
nvidia-curand-cu12
nvidia-cufft-cu12
nvidia-cusparse-cu12
nvidia-cusolver-cu12
nvidia-nvjitlink-cu12
nvidia-cuda-runtime-cu12
